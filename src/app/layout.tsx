import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/header";
import Footer from "@/components/footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Aromas Noor - Fragrâncias Artesanais Exclusivas",
  description: "Desperte seus sentidos com nossas fragrâncias exclusivas. Velas, difusores, sabonetes e óleos essenciais criados artesanalmente para transformar seu ambiente.",
  keywords: ["aromas", "fragrâncias", "velas", "difusores", "óleos essenciais", "sabonetes artesanais", "aromaterapia"],
  authors: [{ name: "Aromas Noor" }],
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-br">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen flex flex-col`}
      >
        <Header />
        <main className="flex-1">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
